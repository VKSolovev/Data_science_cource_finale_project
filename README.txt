Проект состоит из 3 частей. Глобально, во всем проекте осуществлен сбор и визуализация финансовых данных.
Первая часть это файл Part_one.ipynd. Там загружаются данные и создается база данных sql. В этом файле набирается
порядка 80-100 осмысленных строк.
Вторая часть это папка Streamlit (открывать как проект в pycharm), в которой осущественно создание приложения и выгрузка его в интернет.
Запускать там ничего не надо, надо просто посмотреть на инструменты, что я использую
Ссылка на приложение https://infinite-plateau-35794.herokuapp.com
В том же питоновском файле набирается 70 уникальных, осмысленных строк за вычетом коментариев).
Третья часть это визуализация данных с помощью R. 

Ниже приведено примерное разбиение по баллам, в коде написано что и где используется.

Баллы полученны за:
Обработка данных с помощью pandas. 2
Веб-скреппинг. 1
Работа с REST API (XML/JSON). 2 — нетривиальное использование API, существенно выходящее за рамки обсуждавшегося в домашних работах.
Визуализация данных. 1 — базовые визуализации типа «построен scatter plot»
Streamlit 2 — проект размещён в интернете (скачивать ничего не надо, достаточно пройти по ссылке и всё запустится).
SQL 1 — использовался
R 1 — использовался
Визуализация с помощью ggplot2 2 — более сложная визуализация (картинка состоит из нескольких «слоёв» (geom_*)). (При наличии идентичного или похожего кода для Python с библиотекой plotnine и R с библиотекой ggplot2 баллы за ggplot2 не ставятся.)
Визуализация с помощью дополнений к ggplot2 1 — использовалось любое дополнение.
Использовались какие-то технологии, необходимые для реализации проекта, и не обсуждавшиеся в курсе. 1 — использовались немножко (tqdm в первой части проекта)
Объём (осмысленных строк кода). 1 — 120+.
Целостность проекта. 1 — проект выглядит целостным, все технологии используются по делу.
